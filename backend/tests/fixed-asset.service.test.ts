import{describe,expect,it}from"vitest";import{FixedAssetService}from"../src/modules/fixed-asset/fixed-asset.service.js";
describe("FixedAssetService",()=>{const valid={assetNo:"FA-1",name:"电脑",category:"电子设备",purchaseDate:"2026-01-01",startUseDate:"2026-01-02",originalValue:"10000",residualRate:"0.05",depreciationMethod:"STRAIGHT_LINE",usefulLifeMonths:60};it("only allows administrators to create assets",async()=>{const service=new FixedAssetService({}as any);await expect(service.create(valid,{actorId:1,role:"CASHIER"})).rejects.toMatchObject({code:"FORBIDDEN"});});it("rejects invalid residual rates",async()=>{const service=new FixedAssetService({}as any);await expect(service.create({...valid,residualRate:"1"},{actorId:1,role:"ADMIN"})).rejects.toMatchObject({code:"INVALID_FIXED_ASSET"});});it("rejects a use date before purchase",async()=>{const service=new FixedAssetService({}as any);await expect(service.create({...valid,startUseDate:"2025-12-31"},{actorId:1,role:"ADMIN"})).rejects.toMatchObject({code:"INVALID_FIXED_ASSET_DATE"});});it("requires a valid disposal type",async()=>{const service=new FixedAssetService({}as any);await expect(service.dispose(1,{disposalType:"OTHER",disposalDate:"2026-07-01"},{actorId:1,role:"ADMIN"})).rejects.toMatchObject({code:"INVALID_DISPOSAL_TYPE"});});it("does not allow proceeds on a discard",async()=>{const service=new FixedAssetService({}as any);await expect(service.dispose(1,{disposalType:"DISCARD",disposalDate:"2026-07-01",proceeds:"1"},{actorId:1,role:"ADMIN"})).rejects.toMatchObject({code:"INVALID_DISPOSAL_PROCEEDS"});});});
