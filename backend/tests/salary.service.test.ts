import{describe,expect,it,vi}from"vitest";import{Prisma}from"../src/generated/prisma/client.js";import{SalaryService}from"../src/modules/salary/salary.service.js";
describe("SalaryService",()=>{const periods={assertVoucherOperation:vi.fn()}as any;it("only allows administrators to generate salaries",async()=>{const service=new SalaryService({}as any,periods);await expect(service.generate(1,{expenseAccountId:2,payableAccountId:3},{actorId:4,role:"ACCOUNTANT"})).rejects.toMatchObject({code:"FORBIDDEN"});});it("aggregates configured salary items with Decimal",()=>{const service=new SalaryService({}as any,periods);const result=(service as any).itemBuckets([{category:"BONUS",defaultAmount:new Prisma.Decimal("100.10")},{category:"ALLOWANCE",defaultAmount:new Prisma.Decimal("50.20")},{category:"DEDUCTION",defaultAmount:new Prisma.Decimal("20.30")}]);expect(result.bonus.toString()).toBe("100.1");expect(result.allowance.toString()).toBe("50.2");expect(result.deduction.toString()).toBe("20.3");});it("rejects negative or over-precision salary amounts",async()=>{const service=new SalaryService({}as any,periods);await expect(service.createEmployee({employeeNo:"E1",name:"甲",baseSalary:"-1"},{actorId:4,role:"ADMIN"})).rejects.toMatchObject({code:"INVALID_SALARY_AMOUNT"});});});
